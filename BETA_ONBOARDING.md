# GCI ベータ参加者ガイド

> **Global Card Index（GCI）** クローズドβへようこそ。  
> このガイドでは、サービスの読み方・使い方・既知の制限・フィードバックの送り方を説明します。

---

## GCI とは？

GCI は **トレーディングカードの市場価格透明性インフラ** です。

メルカリ等の取引データを収集・集計し、カードごとに「指数値（Index）」と「信頼度（Confidence）」を算出して提供します。株価指数のように、カード相場の全体像をひとつの数字で把握できることを目指しています。

**現在の対応ゲーム**
- Pokémon TCG（ポケモンカード）
- One Piece Card Game（ワンピースカード）

---

## 主要ページの使い方

### ホーム（`/`）
GCI 指数の現在値と 30 日間の推移グラフを表示します。  
指数は基準値 **1000** からスタートし、市場全体の上昇・下降に連動します。

### Marketboard（`/marketboard`）
追跡中の全カードを一覧で確認できます。

- **信頼できる指数** タブ → 高精度（HIGH / MED）のカードのみ表示
- **参考値 / データ不足** タブ → サンプル少・精度限定のカードを表示

| 列 | 説明 |
|---|---|
| Index | カード固有の指数値（基準 1000） |
| Δ Index | 前回比の変動率 |
| Samples | 計算に使ったサンプル数 |
| Latest ¥ | 直近の観測価格 |
| Δ 30d | 30 日間の価格変動率 |

### Cards（`/cards`）
全追跡カードの一覧。条件（NM / LP / MP / HP / DMG）と信頼度でフィルタリングできます。  
カード名をクリックするとカード詳細ページへ移動します。

### カード詳細（`/cards/[slug]`）
個別カードの価格推移・指数値・信頼度を確認できます。

- **ウォッチリストに追加**（☆ ボタン）すると `/watchlist` で一括管理できます
- 信頼度が LOW の場合は「参考値」ラベルが表示されます（データが少ない状態）

### Daily（`/daily`）
毎日更新される市場サマリー。上昇・下落トップ 5、出来高スパイクなどを掲載します。  
過去の日次レポートは `/daily/2026-05-01` のようにアーカイブから参照可能です。

### Trending / Gainers / Losers（`/trending`, `/gainers`, `/losers`）
30 日間の変動率でソートしたカードランキングです。

### Watchlist（`/watchlist`）
気になるカードを登録して一覧管理できます。ブラウザのローカルに保存されるため、デバイスをまたいでの同期はベータ版では対応していません。

---

## 信頼度（Confidence）の読み方

指数値には必ず「どのくらい信頼できるか」を示すラベルが付きます。

| ラベル | 基準 | 意味 |
|---|---|---|
| 🟢 **HIGH** | サンプル 10 件以上 & 外れ値率 < 20% | 最も信頼性が高い |
| 🟡 **MED** | サンプル 3 件以上 & 外れ値率 < 40% | 参考として有効 |
| 🔴 **LOW / 参考値** | 上記に満たない | 目安としての参照に |
| — | サンプル 3 件未満 | 指数値は非表示 |

**なぜ HIGH が少ないのか？**  
ベータ開始直後はデータ収集が始まったばかりです。毎日コレクターが動き、カードごとのデータが蓄積されると HIGH 信頼度のカードが増えていきます。

---

## データの仕組み

1. **収集（毎日）** — メルカリの取引データを手動 + 半自動で収集
2. **フィルタリング** — IQR（四分位範囲）で外れ値を除去し、TrustScore で信頼性を重み付け
3. **指数計算（夜間自動実行）** — 全カードのインデックス値を再計算
4. **Discord 通知** — ±10% 以上の変動があったカードは `#market-alerts` に自動投稿

---

## 既知の制限（ベータ版）

1. **データソースはメルカリのみ** — TCGPlayer・eBay 等は対応予定
2. **価格更新は夜間 1 回** — 日中の取引はその日の夜に反映
3. **グレーディング（PSA 等）非対応** — 生カードのコンディション別のみ
4. **Watchlist はローカル保存** — ブラウザが変わるとリセット
5. **一部カードはデータ不足** — 希少すぎるカードや流通量が少ないカードは LOW / 非表示
6. **アルゴリズムは改善中** — 閾値・重み付けは予告なく変更される場合があります

---

## フィードバックの送り方

ベータ版改善のため、以下でフィードバックをお待ちしています。

### Discord（推奨）
- `#beta-feedback` — 不具合・要望・気になる点は何でも
- `#market-alerts` — 市場の大きな動き（自動投稿）
- `#rising-cards` — 上昇カード通知（自動投稿）
- `#collector-log` — 日次収集サマリー（自動投稿）

### 特に教えてほしいこと
- 「このカードを追加してほしい」→ カード名・セット名を Discord へ
- 「この価格はおかしい」→ スクショ付きで `#beta-feedback` へ
- 「こういう機能がほしい」→ 何でも歓迎

---

## よくある質問

**Q. 価格が実際の相場と違う気がする**  
A. 外れ値除去・サンプル平均の影響で、表示価格が最新の取引価格と異なる場合があります。特に LOW 信頼度のカードはご注意ください。

**Q. カードが見つからない**  
A. まだ追跡リストに登録されていない可能性があります。`#beta-feedback` でリクエストしてください。

**Q. 投資判断に使えますか？**  
A. GCI の情報は参考データです。実際の売買判断はご自身の責任でお願いします。GCI は投資助言サービスではありません。詳しくは[利用規約](/terms)をご確認ください。

**Q. データはいつ更新されますか？**  
A. 指数は毎晩 0 時頃に自動再計算されます。価格収集は毎日手動 + 半自動で行っています。

---

## 利用規約・免責事項

[利用規約・免責事項 →](/terms)

GCI はトレーディングカード市場の**価格透明性**を目的とした情報提供サービスです。掲載情報を参考にした取引の結果について、GCI 運営者は責任を負いかねます。

---

*Global Card Index — Public Beta*  
*ご参加ありがとうございます。*
