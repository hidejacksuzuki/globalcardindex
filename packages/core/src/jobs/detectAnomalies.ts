/**
 * Future: scan recent Price rows for anomalous values
 * (huge spikes, suspect sources, duplicate listings).
 */

export async function detectAnomalies(): Promise<never> {
  throw new Error("detectAnomalies job not implemented");
}
