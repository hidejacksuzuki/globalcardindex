/**
 * Future: collect listings from Mercari.
 * Out of scope for MVP — kept as a typed placeholder so the collector
 * surface is consistent across sources.
 */

export async function collectFromMercari(): Promise<never> {
  throw new Error("mercari collector not implemented");
}
